<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyts.assemble.emailSender.core.mapper.IEmailSendConfigMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hyts.assemble.emailSender.core.bean.EmailSendConfigEntity">
        <id column="id" property="id" />
        <result column="namespace" property="namespace" />
        <result column="protocol" property="protocol" />
        <result column="hostname" property="hostname" />
        <result column="port" property="port" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="template" property="template" />
        <result column="encoding" property="encoding" />
        <result column="enable" property="enable" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, namespace, protocol, hostname, port, username, password, template, encoding, enable
    </sql>

</mapper>
